# Progressive Materialization Notes

Use this reference when the task is about loading only the relevant amount of skill context.

Key reminders:

- Load metadata before instructions.
- Materialize references only when they match the task.
- Apply a character budget before injecting large supporting files.
- Prefer summaries for large resources so the host stays responsive.
