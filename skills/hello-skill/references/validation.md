# Validation Notes

Use this reference when the task is about checking package structure, metadata quality, or permission declarations.

Key reminders:

- `SKILL.md` must start with frontmatter delimited by `---`.
- `name` and `description` are required metadata fields.
- Permission entries should use `capability:scope:mode`.
- Optional resource directories should be directories, not files.
